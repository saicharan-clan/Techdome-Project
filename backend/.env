# MongoDB connection string (use placeholders for username and password)
DB=mongodb+srv://${MONGO_DB_USER}:${MONGO_DB_PASSWORD}@cluster0.mongodb.
net/techdome?retryWrites=true&w=majority
# Backend server port
PORT=5000
# Secret key for encryption (use a placeholder and generate securely)
SECRET=${SECRET_KEY}
# Cloudinary configuration (use environment variables)
CLOUD_NAME=${CLOUDINARY_CLOUD_NAME}
API_KEY=${CLOUDINARY_API_KEY}
API_SECRET=${CLOUDINARY_API_SECRET}
